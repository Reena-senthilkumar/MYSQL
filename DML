DML
DML(DATA  MANIPULATION LANGUAGE)
That is include INSERT,DELETE,UPDATE
•	##INSERT
To add row in table:
                    INSERT INTO student VALUES(“reena”,44,”123 sumit nagar”);
•	##DELETE 
To delete the row:
                    DELETE FROM student where name=”reena”;
                       “This will delete row which row contain name reena”
•	##UPDATE	
 To update the table:
                    UPDATE student SET name=”reenasenthilkumar WHERE name=”reena”;
                     “Here the WHERE is condition this will update the new name instead of 
                        Old name”
